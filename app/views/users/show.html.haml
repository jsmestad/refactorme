.profile
  .avatar_title
    = gravatar_for @user, :size => 80
  .page_title
    %h1= @user.login

.big
  %h1 
    REFACTORS:
    %span
      = @user_snippets.length
  
.big
  %h1 
    SNIPPETS:
    %span
      = @user_snippets.length

.additional
  %strong Last Active:
  =h @user.last_login_at.strftime('%m/%d/%y %T')

= link_to 'Edit', edit_account_path if @user == @current_user